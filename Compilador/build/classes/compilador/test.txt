//COMENTARIO LINEA

procedure hola(in int c){
   if(c>1):
    escriba(c);
    c <- (c - 1);
    llame(hola(c));
   else
    escriba('fintdytrytrytrytr');
   endif
}

main{
    int e;
    float m;
    llame(hola(8));
}
